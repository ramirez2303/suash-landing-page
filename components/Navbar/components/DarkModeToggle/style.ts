import { motion } from "motion/react";
import styled from "styled-components";

export const DarkModeToggleContainer = styled(motion.div)`
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    position: relative;
`;
